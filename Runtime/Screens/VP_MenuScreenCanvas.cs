namespace HannibalUI.Runtime.Screens
{
    using HannibalUI.Runtime.Base;

    public class VP_MenuScreenCanvas : VP_Canvas
    {
        public override CanvasType ScreenType => CanvasType.MenuScreen;

        public override void Setup()
        {
            // TODO: wire up this screen (grab child VP_UIObjects, hook buttons, raise UI events).
        }
    }
}
